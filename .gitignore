*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# SQLite local — datos de desarrollo no se publican.
# Si en VPS usas SQLite, el archivo se crea de cero con `php artisan migrate`.
/database/*.sqlite
/database/*.sqlite-journal
/database/*.sqlite-shm
/database/*.sqlite-wal

# Memoria persistente del agente local (.claude). No tiene nada del codigo.
/.claude/

# CLAUDE.md / instrucciones de agente locales (no compartir contexto privado).
/CLAUDE.md
/CLAUDE.local.md
